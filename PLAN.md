# План дій: Module Health Index на одному сервері, без витрат

**Приватний проєкт.** Не від Unitsoft: окремий домен, окрема пошта, окремий git-акаунт.
**Сервер:** 4 vCPU / 8 GB RAM / 80 GB disk. Усі служби локальні.
**Домен:** є — `allservices.one`. Апекс `allservices.one` віддаємо проєкту, наявний ресурс → на субдомен.
**Витрати:** $0.
**Жорсткий зовнішній дедлайн:** 24 вересня 2026 — реліз Odoo 20.
**Поїздки не потрібні.** Механізмом запуску була дата релізу, не присутність у Брюсселі.
Гілка `20.0` з'явиться в публічному git того ж дня — з Києва вона видна так само.

---

## Крок 0 виконано. Ось реальні дані станом на 18.08.2026

Зібрано за 96 секунд, без Docker, лише `git ls-remote` + treeless clone. 184 репозиторії OCA.

| Серія | Модулів у OCA |
|---|---|
| 16.0 | 2 958 |
| 17.0 | 1 843 |
| 18.0 | 2 560 |
| 19.0 | **1 103** |
| 20.0 | **0 гілок** |

**Головна цифра:** з 2 560 модулів, доступних на 18.0, на 19.0 перенесено лише **941 (36.8%)**.
**1 619 модулів (63.2%) не перенесені** — і це через 11 місяців після релізу 19.0.

Ще:
- Паритету 19.0 з 18.0 досягли **8 репозиторіїв зі 183**.
- **916 модулів застрягли на 16.0** і далі не пішли — кандидати в мертві.
- Гілок `20.0` не існує ні в жодному репозиторії OCA. Цей нуль почне рости з 24 вересня.

Найбільші розриви 18.0 → 19.0: `sale-workflow` (103 модулі не перенесено),
`stock-logistics-workflow` (81), `stock-logistics-warehouse` (76), `product-attribute` (75),
`purchase-workflow` (75), `account-invoicing` (51), `partner-contact` (46).

**Що це означає для проєкту:** гіпотеза підтверджена першим же зрізом. Розрив величезний,
публічно не виміряний, і має природний річний цикл. Файли: `data/oca_snapshot.csv`,
`data/oca_modules.csv`.

---

## Ідея, яку дала ця цифра: живий лідерборд міграції

Гілок 20.0 сьогодні нуль. З 24 вересня спільнота почне їх створювати — і кожен мейнтейнер,
кожен партнер і кожен клієнт захоче бачити, як цей нуль росте. Публічна сторінка
**«OCA 20.0 migration tracker»**, що оновлюється щогодини, — це не контент, це табло.
На нього заходять щодня самі, без маркетингу. Такого табло не існує.

---

## Архітектура під 4 vCPU / 8 GB / 80 GB

Один сервер, усе локально, нічого платного.

```
┌─ Caddy (статика + HTTPS Let's Encrypt) ──────────── :80/:443
├─ Postgres 16 (контейнер, tuned для одноразових БД) ─ 1.5 GB RAM
├─ orchestrator (Python, черга в Postgres) ────────── 0.3 GB RAM
├─ 2 × odoo runner (контейнер на задачу, --memory=2g) 4.0 GB RAM
└─ генератор статики (Python) + JS-пошук у браузері ─ 0.2 GB RAM
```

### Бюджет RAM (8 GB)
| Компонент | RAM |
|---|---|
| ОС + Docker | 0.6 GB |
| Postgres (`shared_buffers=1GB`) | 1.5 GB |
| 2 × Odoo runner по `--memory=2g` | 4.0 GB |
| Оркестратор + Caddy + генератор | 0.5 GB |
| **Запас** | **1.4 GB** |

Більше двох воркерів не ставити: третій з'їсть запас і викличе OOM. Плюс 4 GB swap-файл
як страховка (безкоштовно).

### Бюджет диску (80 GB)
| Що | GB |
|---|---|
| ОС + Docker | 10 |
| Образи odoo 17/18/19 (+20 у вересні) | 9 |
| Чекаути OCA для 18.0 і 19.0 (depth 1) | 6 |
| Postgres: шаблонні БД + робочі | 12 |
| Логи прогонів (gzip, тільки хвіст) | 3 |
| Статичний сайт + JSON-індекс | 1 |
| **Вільно** | **~39** |

### Три оптимізації, без яких на 4 vCPU не вийде

1. **Шаблонні БД.** Один раз створити `tmpl_19` з установленим `base`, далі кожен прогін —
   `CREATE DATABASE job_x TEMPLATE tmpl_19` (1–2 с замість 30–60 с на установку base).
2. **Postgres у режимі «мені не шкода даних».** `fsync=off`, `synchronous_commit=off`,
   `full_page_writes=off`. Дані одноразові — втратити їх не страшно, а швидкість зростає в разы.
   Ці налаштування допустимі **лише** для цієї бази прогонів.
3. **Батчі з бісекцією.** У чистій БД ставити відразу 8 модулів. Пройшло — 8 результатів
   за один прогін. Впало — розділити навпіл і повторити. Більшість батчів проходить,
   тому виграш у 5–8 разів. Модуль з упалого батчу обов'язково переперевіряти окремо.

### Пропускна здатність
- Одиночний прогін модуля з шаблонною БД: ~25–60 с.
- 2 воркери × ~40 с ≈ 3 модулі/хв ≈ **2 500–3 000 модулів/добу**.
- Повний перший прохід (18.0 + 19.0 = 3 663 прогони) ≈ **1,5 доби**. З батчами — за ніч.
- Далі інкрементально: перезапускати модуль лише коли змінився sha його теки. Денна дельта — десятки.

### Плоский пул адонів
Замість довгого `--addons-path` зі 183 тек — символьні лінки:
`pool/19.0/<module> → repos/19.0/<repo>/<module>`. Один шлях, залежності між репозиторіями
резолвяться самі.

---

## Служби: що беремо і чому саме це

| Потреба | Рішення | Чому |
|---|---|---|
| Веб-сервер + HTTPS | **Caddy** (один бінарник) | Let's Encrypt автоматом, нуль конфігурації, ~30 MB RAM |
| БД | **Postgres 16** у контейнері | і черга задач, і результати — без окремого брокера |
| Черга | таблиця в Postgres + `FOR UPDATE SKIP LOCKED` | нуль додаткових служб замість Redis/RabbitMQ |
| Прогони | офіційні образи `odoo:17.0/18.0/19.0` | нічого збирати не треба |
| Генерація сайту | **Python** + шаблони в самому скрипті | без Node, без Hugo — менше залежностей на слабкій машині |
| Пошук по 4 000 модулів | один JSON-індекс + JS у браузері | нуль серверних ресурсів |
| Аналітика | **GoAccess** по логах Caddy, раз на годину в статичний HTML | нуль постійної пам'яті замість Umami/Plausible |
| Моніторинг | systemd + нічний healthcheck-скрипт з листом | нуль додаткових служб |
| Пошта | **SMTP наявної особистої скриньки** | власний MTA на VPS полетить у спам: немає репутації IP і PTR |
| Бекап і offsite | `pg_dump` + експорт CSV/JSON у публічний GitHub-репозиторій | безкоштовно, і це водночас датасет для цитування LLM |

### Домен: апекс віддаємо проєкту
Рішення: **новий проєкт займає апекс `allservices.one`**, наявний ресурс переїжджає на субдомен
(наприклад `app.allservices.one`). Нуль коштів.

Стан DNS на 18.08.2026: A → `65.21.189.197`, **AAAA → мережа OVH (інший хост!)**,
на 443 невалідний сертификат.

Порядок перенесення, готові блоки — у `Caddyfile`:
1. Бекап конфігу наявного веб-сервера **до** будь-яких змін.
2. **Розібратися з AAAA** — це головна пастка. Якщо IPv6 веде на інший сервер,
   Let's Encrypt піде саме туди й сертификат не видасть. Або видалити AAAA, або переставити.
3. A (і AAAA) для субдомену наявного ресурсу → цей сервер.
4. Наявний веб-сервер → `127.0.0.1:8080`, звільнити 80/443.
5. Caddy: апекс → новий сайт, субдомен → `reverse_proxy 127.0.0.1:8080`.
6. Старі шляхи апексу → **301** на субдомен. Без цього поламаються зовнішні посилання
   й будь-які інтеграції, що зверталися до апексу.
7. Перевірити обидва хости зовні, і лише потім прибирати старе.

Під час експериментів — staging-CA Let's Encrypt: ліміт 5 невдалих спроб на домен за годину.

**«Odoo» в імені хоста не використовувати** — обмеження товарного знаку.

### Пошта для приватного проєкту
Власний MTA не піднімати: немає репутації IP і PTR, листи підуть у спам.
- **Системні алерти собі:** SMTP наявної особистої скриньки (Gmail з app password,
  або будь-який ваш провайдер). Нуль коштів, обсяг мізерний.
- **Листи партнерам:** з адреси на `allservices.one`. Для цього потрібні SPF, DKIM і DMARC
  на домені — інакше половина не дійде. Це робота на годину в DNS, але зробити її треба
  **до** першої розсилки, не після.
- Ліміт гігієни: **30–50 листів на день**, тільки персоналізовані. Оскільку проєкт приватний,
  репутація корпоративного домену Unitsoft не під ризиком — але репутація `allservices.one`
  під ризиком, а вона вам ще знадобиться.

---

## Графік до релізу Odoo 20

### Тиждень 1 · 19–25 серпня — сервер живий, дані щоденні
- [ ] **`bash bin/preflight.sh > pf.txt`** — перше, що робимо. Показує зайняті порти,
      чи вже щось стоїть на 80/443, cgroup, вільне місце, DNS, чи працює `docker pull`.
      Без цього розгортання наосліп.
- [ ] DNS: апекс `allservices.one` → IP цього сервера; субдомен для наявного ресурсу; розібратися з AAAA
- [ ] `bin/bootstrap.sh`: Docker, swap 4 GB, теки, логротейт, ліміти
- [ ] Postgres у compose з tuned-конфігом, шаблонні БД для 18.0 і 19.0
- [ ] `indexer/harvest.py` по cron щодня: гілки, модулі, дати комітів (той самий код, що дав дані вище)
- [ ] Caddy + перша сторінка: **OCA 19.0/20.0 tracker** зі вже наявними цифрами
- **Результат тижня:** публічна сторінка з живою цифрою «1 103 модулі на 19.0, 0 на 20.0»
- ~12 годин

### Тижні 2–3 · 26 серпня – 8 вересня — install-харнес
- [ ] `bin/sync_repos.sh`: чекаути 18.0/19.0 + плоский пул симлінками
- [ ] `indexer/runner.py`: БД з шаблону → `docker run odoo -i` → парсинг логу → результат у БД
- [ ] `indexer/classify.py`: класифікатор помилок (OWL/assets, ORM API, залежність, зовнішній python-пакет, таймаут)
- [ ] Батчі з бісекцією
- [ ] Перший повний прохід по 18.0 і 19.0
- **Результат:** фактичний install-статус для ~3 700 модулів
- ~30 годин

### Тиждень 4 · 9–15 вересня — сайт
- [ ] Генератор: ~4 000 сторінок модулів + сторінки репозиторіїв + JSON для пошуку
- [ ] Стабільні URL, JSON-LD, `/llms.txt`, дата прогону на видноті, сирий лог поруч
- [ ] Публічний датасет у GitHub-репозиторій (CSV + JSON, оновлення щодня)
- [ ] Сторінка методології — від неї залежить, чи будуть цитувати
- ~18 годин

### Тиждень 5 · 16–23 вересня — перша публікація
- [ ] Звіт «Готовність OCA до Odoo 19: 63% модулів не перенесено» з сирими даними
- [ ] Публікація: OCA GitHub Discussions, форум Odoo (розділ апгрейду), r/Odoo, LinkedIn, dev.to
- [ ] 50 персональних листів партнерам (не більше 30–50/день) з їхніми власними модулями
- ~18 годин

### 24–29 вересня — вікно унікальності
- [ ] 24-го: у публічному git з'являється `20.0` → того ж дня образ і гілка в харнес
- [ ] Протягом 72 годин — перший у світі зріз сумісності з 20.0
- [ ] Лідерборд міграції на 20.0 стає головною сторінкою, оновлення щогодини
- [ ] Друга хвиля публікацій і листів
- ~20 годин у стислі дні

### Жовтень — гроші
- [ ] Акаунти (magic link на пошту, без паролів), watchlist, приватні репозиторії через deploy key
- [ ] Тижневі алерти
- [ ] Оплати: тут з'явиться перша реальна витрата — Paddle бере 5% + $0.50, але **лише з платежу**,
      абонплати немає. До першого платежу — $0.
- ~50 годин

**Разом до публічного запуску: ~78 годин за 5 тижнів. До платної версії: +50.**

---

## Просування дистанційно, нуль бюджету

1. **Табло, а не контент.** Лідерборд міграції на 20.0 — сторінка, куди мейнтейнери й партнери
   заходять самі, щодня, бо це їхній власний прогрес. Це заміна і SEO, і рекламі.
2. **Цифра як інфопривід.** «63% модулів OCA не перенесені на 19.0» — це факт, який
   перепощують. Один такий факт вартий двадцяти статей. Наступний — по 20.0 у вересні.
3. **Спільнота, де ці люди вже є:** OCA GitHub Discussions і mailing lists, офіційний форум
   Odoo, r/Odoo, LinkedIn-мережа партнерів, Telegram/Discord інтеграторів. Правило: приходити
   з фактом про конкретний модуль, який зараз обговорюють.
4. **Персональні листи.** 30–50 на день з особистої скриньки або адреси на allservices.one (після SPF/DKIM/DMARC), кожен зі звітом по
   модулях саме цього партнера. Це не пітч, а виконана робота.
5. **Вендорські бейджі.** Вендор сам заливає модуль, отримує прогін і бейдж із датою, сам
   ставить його собі на сайт. Ваше посилання розходиться без вашої участі — і так само
   вирішується доступ до платних модулів.
6. **Цитованість у LLM.** Стабільні URL, JSON-LD, `llms.txt`, відкритий датасет у GitHub.
   Партнер питає ChatGPT «is module X ready for Odoo 20» — даних у моделі немає, вона піде шукати.
7. **Чого не робити:** статей «10 найкращих модулів», Google Ads, соцмереж для охоплення,
   власного MTA.

---

## Точка go / no-go — 30 листопада 2026
- 1 000 унікальних відвідувачів-партнерів за 60 днів
- 100 безкоштовних watchlist-акаунтів
- **10 платних підписок** ($39 → ≈$390 MRR)
- ≥5 відповідей із 300 персональних листів

Менше 5 платних — партнери не купують інформацію, вони купують роботу. Тоді індекс
залишається безкоштовним лідгеном, а гроші переходять у платну міграцію модулів
(done-for-you), де вже є підтверджений цінник $3–20k за модуль.

---

## Чесна поправка до попередніх висновків

У OCA **є** інструмент механічної міграції — `OCA/odoo-module-migrator` (і `oca-port` для
перенесення комітів між гілками). Раніше я казав, що інструментів немає — правильне
формулювання: немає **AI**-інструменту і немає **індексу фактичної сумісності**.
Це навіть краще: `odoo-module-migrator` стає вашим детермінованим шаром, а AI підключається
тільки там, де механіка не справляється. І цифра 63% доводить, що наявних інструментів
недостатньо — інакше розриву б не було.

---

## Ризики виконання
- **Брудні прогони.** Частина падінь — через відсутні зовнішні python-пакети, а не через
  несумісність. Класифікатор помилок мусить розділяти це з першого дня, інакше цифрам не повірять.
- **8 GB — межа.** Тільки два воркери, обов'язкові `--memory` ліміти на контейнери, swap 4 GB.
- **Платні модулі** без ліцензії не протестувати — для них лише метадані, і це позначати явно.
- **Репутація домену.** Ліміт на листи, тільки персоналізовані.
- **Юридично:** публікувати факт прогону з логом і датою, а не оцінку якості вендора.
